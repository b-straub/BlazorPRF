name: Publish to NuGet

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install wasm-tools workload
        run: dotnet workload install wasm-tools

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create local NuGet folder
        run: mkdir -p .nuget

      # Install all npm dependencies
      - name: Install npm dependencies (Noble.Crypto)
        working-directory: BlazorPRF.Noble.Crypto/TypeScript
        run: npm install

      - name: Install npm dependencies (BC.Crypto)
        working-directory: BlazorPRF.BC.Crypto/TypeScript
        run: npm install

      - name: Install npm dependencies (Wasm.Crypto)
        working-directory: BaseCrypto/BlazorPRF.Wasm.Crypto/TypeScript
        run: npm install

      # Build all projects first (without packing) to ensure TypeScript is compiled
      - name: Build Shared.Crypto
        run: dotnet build BlazorPRF.Shared.Crypto/BlazorPRF.Shared.Crypto.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Build Noble.Crypto (no pack)
        run: dotnet build BlazorPRF.Noble.Crypto/BlazorPRF.Noble.Crypto.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Verify Noble.Crypto wwwroot exists
        run: ls -la BlazorPRF.Noble.Crypto/wwwroot/

      - name: Build BC.Crypto (no pack)
        run: dotnet build BlazorPRF.BC.Crypto/BlazorPRF.BC.Crypto.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Verify BC.Crypto wwwroot exists
        run: ls -la BlazorPRF.BC.Crypto/wwwroot/

      - name: Build Wasm.Crypto (no pack)
        run: dotnet build BaseCrypto/BlazorPRF.Wasm.Crypto/BlazorPRF.Wasm.Crypto.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Build Server.Crypto (no pack)
        run: dotnet build BaseCrypto/BlazorPRF.Server.Crypto/BlazorPRF.Server.Crypto.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      # Now pack the crypto packages (static web assets should be discovered)
      - name: Pack Noble.Crypto
        run: dotnet pack BlazorPRF.Noble.Crypto/BlazorPRF.Noble.Crypto.csproj -c Release --no-build -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Verify Noble.Crypto package contents
        run: unzip -l .nuget/BlazorPRF.Noble.Crypto.${{ steps.version.outputs.VERSION }}.nupkg

      - name: Pack BC.Crypto
        run: dotnet pack BlazorPRF.BC.Crypto/BlazorPRF.BC.Crypto.csproj -c Release --no-build -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Pack Wasm.Crypto
        run: dotnet pack BaseCrypto/BlazorPRF.Wasm.Crypto/BlazorPRF.Wasm.Crypto.csproj -c Release --no-build -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Pack Server.Crypto
        run: dotnet pack BaseCrypto/BlazorPRF.Server.Crypto/BlazorPRF.Server.Crypto.csproj -c Release --no-build -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: List local NuGet packages
        run: ls -la .nuget/

      - name: Clear all NuGet caches
        run: dotnet nuget locals all --clear

      # Build UI with both crypto engine flavors (no pack first)
      - name: Build UI (Noble, no pack)
        run: dotnet build BlazorPRF.UI/BlazorPRF.UI.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFCryptoEngine=Noble -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Pack UI (Noble)
        run: dotnet pack BlazorPRF.UI/BlazorPRF.UI.csproj -c Release --no-build -p:BlazorPRFCryptoEngine=Noble -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Build UI (BC, no pack)
        run: dotnet build BlazorPRF.UI/BlazorPRF.UI.csproj -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFCryptoEngine=BC -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Pack UI (BC)
        run: dotnet pack BlazorPRF.UI/BlazorPRF.UI.csproj -c Release --no-build -p:BlazorPRFCryptoEngine=BC -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: List all NuGet packages
        run: ls -la .nuget/

      # Build and test solution (without regenerating packages)
      - name: Build solution
        run: dotnet build BlazorPRF.slnx -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }}

      - name: Test
        run: dotnet test BlazorPRF.slnx -c Release -p:GeneratePackageOnBuild=false -p:BlazorPRFVersion=${{ steps.version.outputs.VERSION }} --no-build --verbosity normal

      # Copy packages to ./nupkg for publishing
      - name: Copy packages
        run: |
          mkdir -p ./nupkg
          cp .nuget/*.nupkg ./nupkg/

      - name: List packages to publish
        run: ls -la ./nupkg/

      - name: Verify Noble.Crypto package before upload
        run: unzip -l ./nupkg/BlazorPRF.Noble.Crypto.${{ steps.version.outputs.VERSION }}.nupkg

      - name: Verify BC.Crypto package before upload
        run: unzip -l ./nupkg/BlazorPRF.BC.Crypto.${{ steps.version.outputs.VERSION }}.nupkg

      - name: Push to NuGet
        run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: ./nupkg/*.nupkg
          generate_release_notes: true
